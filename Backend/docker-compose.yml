version: "3.8"

services:
  backend:
    build: .
    container_name: backend_rest_node
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped